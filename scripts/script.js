import { listaDoCarrinho } from "./database.js";

// Implemente a função removeDoCarrinho que recebe um produto como parâmetro e remove esse produto do carrinho.
export function removeDoCarrinho(produto) {
  /* desenvolva sua lógica aqui */
  // const removed  = listaDoCarrinho

  // Certifique-se de que o produto é removido do carrinho usando o método splice.
  // Lembre-se de que você deve retornar o produto removido no return da função.

  return /* Lembre-se de retornar o produto removido aqui */;
}
